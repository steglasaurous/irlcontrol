import { ChatMessageEmotesPipe } from './chat-message-emotes.pipe';

describe('ChatMessageEmotesPipe', () => {
  it('create an instance', () => {
    const pipe = new ChatMessageEmotesPipe();
    expect(pipe).toBeTruthy();
  });
});
